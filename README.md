# Rahul H — Portfolio

This repository contains a personal portfolio website for Rahul H. It is a static site built with plain HTML, CSS and JavaScript.

Run locally
```
cd "c:\Users\rahul\OneDrive\Desktop\pp\my portfolio"
python server.py
# then open http://localhost:8000
```

Deploy (GitHub Pages)

This repository includes a GitHub Actions workflow that automatically deploys the repository root to GitHub Pages on every push to `main`.

Live URL (once Pages is configured):

`https://RAhul-pro-code.github.io/portfolio`

Notes
- Update project links in `index.html` if repository names change.
- You can also deploy to Vercel or Netlify for more features.
